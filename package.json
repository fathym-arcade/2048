{
  "name": "@fathym-arcade/twenty48",
  "version": "0.0.5",
  "scripts": {
    "build": "",
    "deploy": "npm version patch && npm run deploy:app",
    "deploy:app": "npm run build && npm publish ./ --access public"
  }
}
